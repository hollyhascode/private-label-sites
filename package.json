{
  "name": "hello-render-monorepo",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "devDependencies": {
    "eslint": "^6.8.0",
    "prettier": "^1.19.1",
    "rimraf": "^3.0.0"
  },
  "scripts": {
    "postinstall": "yarn run setup",
    "setup": "yarn ui install",
    "ui": "cd ui && yarn"
  }
}
